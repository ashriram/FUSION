/** Auto generated C++ code started by symbols/Func.C:125 */

#include "Types.h"
#include "Chip.h"
#include "L2Cache_Controller.h"

bool L2Cache_Controller::L2Cache_isOneSharerLeft(Address param_addr, MachineID param_requestor)
{
    if (ASSERT_FLAG && !((false))) {
    cerr << "Runtime Error at ../protocols/FUSION/cachelease2-L2cache_m.sm:307, Ruby Time: " << g_eventQueue_ptr->getTime() << ": " << "assert failure" << ", PID: " << getpid() << endl;
char c; cerr << "press return to continue." << endl; cin.get(c); abort();

  }
;
  return (false);
}

