/** Auto generated C++ code started by symbols/Func.C:125 */

#include "Types.h"
#include "Chip.h"

GenericRequestType Chip::convertResponseToGenericType(CoherenceResponseType param_type)
{
  DEBUG_SLICC(MedPrio, "../protocols/FUSION/MESI_CMP_directory-msg.sm:125: ", param_type);
;
    cerr << "Runtime Error at ../protocols/FUSION/MESI_CMP_directory-msg.sm:126, Ruby Time: " << g_eventQueue_ptr->getTime() << ": " << ("invalid CoherenceResponseType") << ", PID: " << getpid() << endl;
char c; cerr << "press return to continue." << endl; cin.get(c); abort();

;
}

