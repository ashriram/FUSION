Have to run make in jpeg-6a directory --  make all -j 32
Do NOT run configure before running make. Profile flags are embedded in the Makefile, remove and recompile before tracing.

Check the runme_large.sh script for invocation params. 

cjpeg -- Convert to jpg, input is ppm
djpeg -- Convert to ppm, input is jpg


